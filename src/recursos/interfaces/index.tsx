export interface ActividadesReporteParcial {
    id: number,
    descripcion: string,
    cantidad: number[]
}

export interface AtencionesRealizadas {
    descripcion: string,
    cantidad: number[]
}
